document.addEventListener("DOMContentLoaded", () => {
   
    for (let index = 0; index < 100; index++) {
        console.log(index);
    }

    let index = 0;
    while ( index < 100) {
        console.log(index);
        index++;
    }

    do {
        console.log(index);
        index++;
    } while ( index < 100)

    
    
});